select count(*) from doctors_specs;
-- +-------+
-- | count |
-- +-------+
-- |    55 |
-- +-------+
-- (1 строка)


select sum(salary) from doctors;
-- +------------+
-- |    sum     |
-- +------------+
-- | 4124559.49 |
-- +------------+
-- (1 строка)


select sum(salary + premium) from doctors;
-- +------------+
-- |    sum     |
-- +------------+
-- | 5732805.32 |
-- +------------+
-- (1 строка)


select avg(salary + premium) from doctors;
-- +--------------------+
-- |        avg         |
-- +--------------------+
-- | 81897.218857142857 |
-- +--------------------+
-- (1 строка)


select string_agg(name, ', ') from wards;
-- +--------------------------------------------------------------------------------------------------------------------+
-- |                                                     string_agg                                                     |
-- +--------------------------------------------------------------------------------------------------------------------+
-- | ООТ-1, ООТ-2, ООТ-3, ООТ-4, НО-1, НО-2, КО-1, КО-2, ОФД-1, ОФД-2, РИТ-1, РИТ-2, ТО-1, ТО-2, ТО-3, ТО-4, ФО-1, ФО-2 |
-- +--------------------------------------------------------------------------------------------------------------------+
-- (1 строка)


